<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Meal History - HealthyLife</title>
    <link rel="stylesheet" type="text/css" href="css/style.css" />
    <link rel="stylesheet" type="text/css" href="css/mealHistory.css" />
</head>
<body>
    <div id="page">
        <%- include('../partials/header') %>
        <div id="content">
            <div>
                <h1>Meal History</h1>
                <p>Welcome to your Meal History page! Here you can view a record of the meals you've consumed over time.</p>
                
                <form id="filter-form" class="filter-form">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="start-date">Start Date:</label>
                            <input type="date" id="start-date" name="startDate" required>
                        </div>

                        <div class="form-group">
                            <label for="end-date">End Date:</label>
                            <input type="date" id="end-date" name="endDate" required>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <input type="submit" value="Filter History" class="submit-button">
                        </div>
                    </div>
                </form>

                <table class="meal-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Meal Type</th>
                            <th>Description</th>
                            <th>Image</th>
                            <th>Meal Date</th>
                            <th>Blood Sugar</th>
                            <th>Food Sugar</th>
                            <th>Is Holiday</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Data will be dynamically generated here -->
                    </tbody>
                </table>

                <p>If you have any further questions or need assistance, don't hesitate to reach out. We are here for you!</p>
            </div>
        </div>
        <%- include('../partials/footer') %>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const filterForm = document.getElementById('filter-form');
            const tableBody = document.querySelector('.meal-table tbody');
    
            // טעינה אוטומטית של הארוחות בעת טעינת העמוד
            fetchMealHistory(true);
    
            filterForm.addEventListener('submit', function(event) {
                event.preventDefault();
                fetchMealHistory();
            });
    
            function fetchMealHistory(viewAll = false) {
                let url = '/meals/history';
                if (!viewAll) {
                    const startDate = document.getElementById('start-date').value;
                    const endDate = document.getElementById('end-date').value;
                    url += `?startDate=${startDate}&endDate=${endDate}`;
                }
    
                fetch(url)
                    .then(response => {
                        if (response.status === 401) {
                            window.location.href = '/?message=' + encodeURIComponent('Please login first');
                            throw new Error('Not authenticated');
                        }
                        if (!response.ok) {
                            throw new Error('Network response was not ok');
                        }
                        return response.json();
                    })
                    .then(data => {
                        displayMealHistory(data);
                    })
                    .catch(error => {
                        if (error.message !== 'Not authenticated') {
                            console.error("Error fetching meals:", error);
                            alert("Error getting meal history. Please try again.");
                        }
                    });
            }
    
            function displayMealHistory(meals) {
                tableBody.innerHTML = '';
                meals.forEach(meal => {
                    const row = document.createElement('tr');
                    row.innerHTML = `
                        <td>${meal.id}</td>
                        <td>${meal.mealType}</td>
                        <td>${meal.description}</td>
                        <td><img src="${meal.image}" alt="${meal.mealType}" style="width: 50px; height: auto;"></td>
                        <td>${new Date(meal.mealDate).toLocaleDateString()}</td>
                        <td>${meal.bloodSugar}</td>
                        <td>${meal.foodSugar || 'N/A'}</td>
                        <td>${meal.isHoliday ? 'Yes' : 'No'}</td>
                    `;
                    tableBody.appendChild(row);
                });
            }
        });
    </script>
</body>
</html>
